<h2>ハッシュタグ #<%= @tag.hashname %></h2>
  <ul>
    <% @books.each do |book| %>
      <li>
        <%= link_to book_path(book.id) do %>
          <%= book.body %>
        <% end %>
      </li>
    <% end %>
  </ul>